[database]
	host = "localhost"
	username = "<%= user %>"
	password = "<%= pw %>"
	database = "<%= db %>"
[web]
; set to /testswarm to run a local copy in a non-root directory, also update .ht
access with RewriteBase /testswarm/
	contextpath = ""
